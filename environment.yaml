name: chirpp_gpu
channels:
  - pytorch
  - nvidia
  - defaults
  - conda-forge
dependencies:
  - pip
  - torch
  - python=3.10
  - postgresql
  - pip:
      - torch
  - medspacy
variables:
  PYTHONNOUSERSITE: '0'